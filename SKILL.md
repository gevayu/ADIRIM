---
name: bni-adirim-guests
description: ניהול הרשימה השבועית של מבקרים ואורחים בצ'פטר BNI אדירים מקצה לקצה - איפוס ועדכון קובץ ה-HTML השבועי, עדכון תאריכים, נרמול טלפונים, סיווג type נכון, אימות חברות BNI דרך דפדפן אמיתי (Playwright), חישוב סטטיסטיקות, ובניית הפלט. יש להשתמש בסקיל בכל פעם שיובל מעלה או מדביק רשימת מבקרים/אורחים של BNI, מבקש לאמת חברות BNI, מבקש לאפס/לעדכן את כלי ניהול המבקרים של אדירים, מבקש לעדכן תאריך למפגש, או מזכיר "מבקרים", "אורחים", "צ'פטר", "אדירים" או "BNI" בהקשר של טבלה שבועית. להפעיל גם אם יובל לא אומר במפורש "אמת חברות", כל עוד יש רשימת מבקרים לעבד.
---

# ניהול מבקרים שבועי, צ'פטר BNI אדירים

תהליך שבועי חוזר, מקצה לקצה. מקבלים רשימת מבקרים ואורחים, מאמתים מי חבר BNI פעיל, מסווגים כל אחד, ובונים קובץ HTML מוכן לצ'פטר. הכל דטרמיניסטי דרך שני סקריפטים, בלי לנחש ובלי לשאול שאלות מיותרות.

הטעויות היקרות: לאשר חברות למי שאינו חבר, ולשנות בטעות "ממלא מקום" ל"מבקר". שני החוקים מפורטים למטה, אל תעגל אותם.

## מבנה הסקיל

- `assets/template.html` - תבנית ה-HTML עם טוקנים. אל תערוך ידנית, `build.py` ממלא אותה.
- `references/bni_verify.py` - אימות חברות BNI דרך Playwright.
- `references/build.py` - בונה את ה-HTML הסופי מקובץ JSON.
- `references/verification.md` - מקרי קצה של אימות. קרא בעת ספק.

## התהליך המלא

### שלב 1, קליטת הנתונים

הנתונים מגיעים כטבלה או טקסט מודבק. שדות אפשריים: שם פרטי, שם משפחה, עיסוק/חברה, מי הזמין, טלפון, דוא"ל, וסוג במקור ("אורח", "מבקר/ת", "ממלא מקום"). לפעמים יש עמודת "חבר BNI".

**הסוג במקור והעמודה "חבר BNI" אינם קובעים חברות.** תמיד לאמת בנפרד בשלב 2. אל תסמוך על מה שכתוב בעמודה.

המר כל שורה לרשומת JSON:
```json
{ "first":"", "last":"", "company":"", "inviter":"", "phone":"", "email":"", "type":"guest|sub|visitor", "gender":"m|f" }
```
- `gender` נקבע לפי התואר (גברת/מר/העלמה/דר'/פרופ') כשקיים, אחרת לפי הגיון השם. חשוב לקביעת אורח/אורחת נכון, במיוחד לשמות יוניסקס (שרון, וכו').
- שמות דו-לשוניים ("תומר Tomer") - קח את החלק העברי בלבד לרשומה.
- אל תמלא טלפון ידנית, `build.py` מנרמל.

### שלב 2, אימות חברות BNI

**חובה Playwright מול הטופס הרשמי. אסור גוגל/web_search** - גוגל לא מאנדקס את רוב עמודי memberdetails, ומחזיר "לא נמצא" שגוי גם על חברים ודאיים (פספס בעבר את שניר אוזנוביץ, רונית שגיב, ריטה מושיאקוב).

הרץ בקבוצות של עד ~5 שמות:
```bash
cd <skill_dir> && timeout 118 python3 references/bni_verify.py "[('שם','משפחה'),('שם','משפחה')]"
```
פלט לכל שם: `MEMBER-EXACT` (התאמת שם פרטי+משפחה מדויקת = חבר מאושר), רשימת שמות דומים, או `NONE`. **רק MEMBER-EXACT מאשר.** שם דומה, אותו עסק או היכרות, לא מספיקים.

חריג יחיד: אם יובל מאשר ישירות חברות בינלאומית (חבר במדינה אחרת שלא באתר הישראלי), סמן `bniMember:true` על סמך אישורו, בלי התאמה בטופס. קישור memberdetails ישיר שהוא שולח הוא גם אישור תקף.

אם Playwright חסר: `pip install playwright && python3 -m playwright install chromium`.

### שלב 3, סיווג type אחרי אימות

הכלל תלוי בסיווג המקורי. **זו הטעות היקרה:**
```
מקור "אורח"     + מאושר  ->  type:"visitor" + bniMember:true
מקור "ממלא מקום" + מאושר  ->  type:"sub"     + bniMember:true   (לא לשנות ל-visitor לעולם)
מקור "מבקר"     + מאושר  ->  type:"visitor" + bniMember:true
כל סיווג        + לא מאושר ->  type נשאר כמו במקור, בלי bniMember
```
הוסף `"bniMember":true` לרשומות ה-JSON של המאושרים בלבד.

### שלב 4, בניית הקובץ

שמור את כל הרשומות לקובץ JSON, והרץ:
```bash
cd <skill_dir> && python3 references/build.py <data.json> <YYYY-MM-DD> <out.html>
```
הסקריפט מנרמל טלפונים ל-0XX-XXX-XXXX, מחשב את ארבעת המונים אוטומטית מ-type, ממלא את כל התאריכים (כותרת, הדר, פוטר, שם קובץ הייצוא), ומזריק את הרשומות. הוא מדפיס סיכום: total/guests/subs/visitors ורשימת חברי BNI. הצג את הקובץ ליובל עם present_files.

"מחר" נקבע לפי התאריך הנוכחי. שים לב שהמפגש בדרך כלל ביום חמישי, אם "מחר" יוצא יום אחר, ציין זאת ליובל.

## איפוס בלבד (בלי נתונים חדשים)

אם יובל מבקש רק לאפס ולעדכן תאריך למפגש הבא: הרץ `build.py` עם קובץ JSON ריק (`[]`) והתאריך החדש. זה מאפס נתונים, מאפס מונים, ומעדכן תאריכים בבת אחת.

## Checklist לפני מסירה

1. כל שם עבר אימות דרך `bni_verify.py`, לא גוגל ולא לפי העמודה/הסיווג במקור.
2. אף sub לא הפך ל-visitor.
3. bniMember:true רק למי שיצא MEMBER-EXACT או שיובל אישר.
4. gender נכון לכל רשומה (בדיקת אורח/אורחת).
5. שמות מדויקים בלי שיבוש. שם יובל הוא יובל, לא יוסי.
6. הרצת build.py הצליחה והמונים בסיכום נראים נכון.
