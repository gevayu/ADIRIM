# Local dev: a libSQL file in the repo. Runs with zero setup.
DATABASE_URL=file:./data/local.db

# Production (Vercel + Turso): set these instead of the file URL above.
# DATABASE_URL=libsql://<your-db>.turso.io
# DATABASE_AUTH_TOKEN=<your-turso-token>

# Editor password. Editing (the "עדכן רשימה" button + the write API) is locked unless set.
# Change this to your own secret in Vercel's env settings.
ADMIN_PASSWORD=change-me
